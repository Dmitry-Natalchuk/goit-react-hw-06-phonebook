/* import { createSlice } from '@reduxjs/toolkit';

const setContacts = [];

export const store = createSlice({
  name: 'contacts',
  initialState: setContacts,
  reducers: {
    addNumber(state, action) {
      state.push(action.payload);
    },
    deleteNumber(state, action) {},
  },
});
 */
